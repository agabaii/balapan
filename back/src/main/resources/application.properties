# Server Configuration
server.port=8081
spring.application.name=balapan-language

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5433/balapan_db
spring.datasource.username=postgres
spring.datasource.password=1105
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
# ВАЖНО: update вместо create-drop чтобы не терять данные!
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Flyway Configuration - ОТКЛЮЧЕНО
spring.flyway.enabled=false

# Connection Pool
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# JWT Configuration
jwt.secret=balapan_super_secret_key_change_in_production_2024
jwt.expiration=86400000

# Logging
logging.level.kz.balapan=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG

# File Upload (for audio files)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Encoding
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=agabaiaida@gmail.com
spring.mail.password=nchy vmoa wjxu vjmo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Custom Email properties
app.mail.from=agabaiaida@gmail.com
app.mail.app-name=Аида Balapan